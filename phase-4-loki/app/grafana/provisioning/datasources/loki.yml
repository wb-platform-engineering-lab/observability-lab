apiVersion: 1
datasources:
  - name: Loki
    type: loki
    uid: loki
    url: http://loki:3100
    isDefault: false
    editable: false
    jsonData:
      # Derived fields: extract the trace_id from a log line and link to Tempo.
      # Uncomment in Phase 5 when Tempo is part of the stack.
      # derivedFields:
      #   - name: TraceID
      #     matcherRegex: '"trace_id":"(\w+)"'
      #     url: http://tempo:3200/jaeger/ui/trace/$${__value.raw}
      #     datasourceUid: tempo
      maxLines: 1000
